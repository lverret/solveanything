u(0, y) = 0
u(1, y) = 0
u(x, 0) = pi * sin(pi * x)
u(x, 1) = -pi * sin(pi * x)
v(0, y) = -pi * sin(pi * y)
v(1, y) = pi * sin(pi * y)
v(x, 0) = 0
v(x, 1) = 0
p(0, 0) = 0
-0.1 * (grad(grad(u, x), x) + grad(grad(u, y), y)) + grad(p, x) = 0.2 * pi**3 * sin(pi * x) * cos(pi * y) + pi * cos(pi * x) * sin(pi * y)
-0.1 * (grad(grad(v, x), x) + grad(grad(v, y), y)) + grad(p, y) = -0.2 * pi**3 * cos(pi * x) * sin(pi * y) + pi * sin(pi * x) * cos(pi * y)
grad(u, x) + grad(v, y) = 0

# Expected function: u = pi * sin(pi * x) * cos(pi * y)
# Expected function: v = -pi * cos(pi * x) * sin(pi * y)
# Expected function: p = sin(pi * x) * sin(pi * y)
